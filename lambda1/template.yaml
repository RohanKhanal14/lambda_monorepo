# AWSTemplateFormatVersion: "2010-09-09"
# Transform: AWS::Serverless-2016-10-31
# Description: Lambda1 Function Stack (SAM) - monorepo

# Parameters:
#   Environment:
#     Type: String
#     Default: Development
#     AllowedValues:
#       - Development
#       - Staging
#       - Production

# Globals:
#   Function:
#     Runtime: python3.11
#     Timeout: 30
#     MemorySize: 128
#     Environment:
#       Variables:
#         LOG_LEVEL: INFO
#         ENVIRONMENT: !Ref Environment
#     LoggingConfig:
#       LogFormat: JSON
#       ApplicationLogLevel: INFO
#     Tags:
#       Environment: !Ref Environment
#       Purpose: test
#       Owner: rohan.khanal
#       Project: GUMP_NOW

# Resources:
#   Lambda1Function:
#     Type: AWS::Serverless::Function
#     Properties:
#       FunctionName: !Sub "lambda1-${Environment}"
#       CodeUri: .
#       Handler: app.lambda_handler
#       Role: !ImportValue
#         Fn::Sub: "lambda1-execution-role-arn-${Environment}"
#       Layers:
#         - !ImportValue
#           Fn::Sub: "lambda1-deps-layer-arn-${Environment}"
#       Tags:
#         Name: !Sub "lambda1-${Environment}"
#         Environment: !Ref Environment
#         Purpose: test
#         Owner: rohan.khanal
#         Project: GUMP_NOW

#   Lambda1LogGroup:
#     Type: AWS::Logs::LogGroup
#     Properties:
#       LogGroupName: !Sub "/aws/lambda/lambda1-${Environment}"
#       RetentionInDays: 7

# Outputs:
#   Lambda1FunctionArn:
#     Value: !GetAtt Lambda1Function.Arn


AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Lambda1 IAM Role and Policies

Parameters:
  Environment:
    Type: String
    Default: Development
    AllowedValues: [Development, Staging, Production]

  RoleHash:
    Type: String
    Description: Hash of role template for change detection

Resources:
  Lambda1ExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "lambda1-execution-role-${Environment}"

      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole

      # Basic CloudWatch Logs permissions
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole

      # Add extra permissions here if needed (SSM, S3, DynamoDB, CodePipeline, etc.)
      Policies:
        - PolicyName: !Sub "lambda1-extra-${Environment}"
          PolicyDocument:
            Version: "2012-10-17"
            Statement: []

      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Purpose
          Value: test
        - Key: Owner
          Value: rohan.khanal
        - Key: Project
          Value: GUMP_NOW
        - Key: RoleHash
          Value: !Ref RoleHash

Outputs:
  ExecutionRoleArn:
    Value: !GetAtt Lambda1ExecutionRole.Arn
    Export:
      Name: !Sub "lambda1-execution-role-arn-${Environment}"
